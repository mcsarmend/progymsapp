name: supplement_app
description: A supplement store app
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  # TEMPORALMENTE: Quita o comenta shared_preferences
  # shared_preferences: ^2.2.2
  provider: ^6.1.2
  http: ^1.1.0
  flutter_svg: ^2.0.9
  shared_preferences: ^2.5.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  # assets:
  #   - assets/images/